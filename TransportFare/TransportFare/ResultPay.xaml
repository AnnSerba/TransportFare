<Page
    x:Class="TransportFare.ResultPay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TransportFare"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel >
            <TextBlock Text="Результаты операции" Style="{StaticResource styleHeaderTextBlock}"/>

            <Frame Name="frameResultsPay" Style="{StaticResource styleFrameResults}">
                <TextBlock  Name="textBlockResultsPay" Text="Не определены" Style="{StaticResource styleTextBlockResults}"/>
            </Frame>
            <TextBlock Text="Коснитесь меткой для синхронизации" Style="{StaticResource styleTextBlock}"/>
            <Frame Name="frameResultsSynh" Style="{StaticResource styleFrameResults}">
                <TextBlock  Name="textBlockResultsSynch" Text="Не синхронизировано" Style="{StaticResource styleTextBlockResults}"/>
            </Frame>
        </StackPanel>
        <Frame Name="frameError" Style="{StaticResource styleFrameError}">
            <TextBlock  Name="textBlockError" Text="" Style="{StaticResource styleTextBlockError}"/>
        </Frame>
    </Grid>
</Page>
